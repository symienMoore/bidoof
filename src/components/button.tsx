import React from 'react'

const Button = (text: string) => {
  return (
    <div>
      <button className='bg-purple-500'>test('should ', () => {
        
      })
      </button>
    </div>
  )
}

export default Button
